<UserControl x:Class="RpgDJ.RenameWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:RpgDJ.ViewModels"
             xmlns:local="clr-namespace:RpgDJ"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:RenameWindowViewModel}"
             Visibility="{Binding RenameWindowVisibility}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Border Background="Transparent" BorderBrush="Gray"  Grid.ColumnSpan="3" Grid.RowSpan="3" BorderThickness="5"></Border>
        <Label Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Foreground="LightGray" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center" >Rename session:</Label>
        <TextBox IsReadOnly="False" IsEnabled="True" Grid.Row="1" Grid.ColumnSpan="3" Margin="10" Text="{Binding SessionName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <Button Grid.Row="2" Grid.Column="1" Margin="15" Command="{Binding SaveCommand}" >Save</Button>
        <Button Grid.Row="2" Grid.Column="2" Margin="15" Command="{Binding CancelCommand}">Cancel</Button>        
    </Grid>
</UserControl>
